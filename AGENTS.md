# プロジェクト共通の管理ID

## 作業対象の固定

このプロジェクトで行う作業対象は `ChordWiki-Bar-Formatter` のみとする。
`AutoChordproEdit` は、ユーザーが明示的に対象として指定しない限り、探索・参照・比較・変更しない。

## 表示不具合の再発防止メモ

- 青いコード文字だけが二重に見える場合は、フォントや`text-stroke`だけで判断せず、`textarea`と`pre.editor-highlight`の同時描画を確認する。
- 色付けONでは`textarea`を透明、色付けOFFでは`editor-highlight`を非表示にし、2層が同時に文字を描画しない状態を保つ。
- CSSを変更したのに実画面へ反映されない場合は、HTMLの`style.css?v=...`キャッシュバスターと、ブラウザが実際に読み込んだCSSの内容を照合する。
- ローカル画面の確認では、ファイルの内容だけでなく、実ブラウザの計算済み`color`・`visibility`・`text-stroke`とCSS URLを確認する。

このプロジェクトで修正、動作確認、設計判断を報告するときは、`MANAGEMENT_IDS.md` を参照する。

- 同じ機能・確認項目には、チャットが変わっても既存の管理IDを再利用する。
- 新しい項目には、該当カテゴリの次の連番を割り当てて `MANAGEMENT_IDS.md` に追記する。
- 既存IDは改番・再利用・削除しない。廃止した場合は状態を「廃止」として残す。
- 最終報告には、関係する管理IDを必ず記載する。
- ユーザーが管理IDを指定した場合は、その台帳項目を作業範囲の基準にする。
- コード位置が役立つ場合は、管理IDに加えてファイル名と行番号も示す。
- Gitコミットを作成するときは、コミットメッセージの先頭に主な管理IDを角括弧で付ける（例：`[ROW-003] 行修正記号を修正`）。

## 変更・検証の共通方針

- 意味が複数に取れる短い追加指示でも、破壊的でなく実装可能なら、現在のリポジトリを対象に妥当な仮定を置いて進める。対象の違いで結果が大きく変わる場合、外部送信や破壊的操作が必要な場合だけ、確認を1問行う。
- 明示された作業依頼は、現在のリポジトリを対象に、妥当な仮定を置いて先に進める。フォルダの場所や作業対象を毎回確認しない。別プロジェクトへ移る必要がある場合、または対象の違いで結果が大きく変わる場合だけ、確認を1問行う。
- UI変更は、現行のDOM・CSS・既存テストと、提供された画面画像があればそれらを先に確認する。実装前に、文章仕様だけでなく「誰が何を押すか」「押した後にどこへ反映されるか」「画面上で何をどこに置くか」を短い操作フローとASCII図・箇条書きなどで共有する。複数のレイアウト案がある場合は、構造（固定する領域、折りたたむ説明、レスポンシブ時の並び）を決めてからまとめて実装する。
- UIの反復調整では、ユーザー確認前の小変更ごとにコミットせず、ひとまとまりの確認可能な差分としてコミットする。変更後は、ボタンの見た目だけでなく、ラベル・`aria-*`・リンク先・操作対象の対応も確認する。
- ブラウザーへ接続できない場合は、実画面を確認したと主張しない。静的検査・関連テスト・ビルドを実施し、未確認の画面幅と接続上の制約を最終報告へ明記する。
- 表示文言を検査するテストは、固定の長文全体ではなく、ID・クラス・操作名・重要語句などの安定した意味を検査する。文言自体が仕様のときだけ全文一致を使う。
- 認証や公開の完了は、ブラウザーの成功画面だけで判断しない。`gh auth status` と対象リモートへの読み取りまたはpush結果で確認し、ローカルのコミット済みとリモート反映済みを分けて報告する。認証失敗時にデバイスコード発行を無制限に繰り返さない。
- 最終確認では、意図したファイルだけが変更されていることを `git status` と `git diff --check` で確認し、関連テストと `npm run build` を通す。単純な文言・配置変更でも、既存テストの意味を壊していないか確認する。
- 複雑な入力記号・parser・変換規則を変更するときは、実装前に代表的な入力・期待出力・境界例を小さな表に固定し、確認済みの例を回帰テストにする。例を増やすだけの試行錯誤を繰り返さない。
- ローカル画面の確認では、対象リポジトリ・起動元・サーバーの準備完了と現在の変更を先に確認し、更新後に固有のDOMや文言で新しい表示を検証する。同じ古い表示や接続失敗を、再起動・強制更新なしに繰り返し確認しない。
- 保存対象となる状態を追加・変更するときは、`docs/SAVED_DATA_SCHEMA.md`で所有する保存形式を確認し、該当するsnapshot／backup／復元テストを更新する。保存形式の`version`を既存データに対して黙って変更しない。
- 「開発基盤を改善して」「品質改善を続けて」など、改善対象を限定しない依頼では`chordwiki-development-orchestrator` Skillを使い、候補を比較して費用対効果の高い小さな改善を1件ずつ実施・検証する。次の価値が低い場合は自律的に終了し、仕様判断・大規模変更・破壊的操作・公開操作は人間へ返す。

## 改善サイクルの標準表示

改善対象を限定しない依頼では、次の1本の流れで進める。

1. 現状確認：読み取り専用で、できていること・未解決点・関係ファイルを確認する。
2. 次にやること：費用対効果と安全性を見て、優先する改善を1〜3件に絞る。
3. 方針：採用する実装方法、変更範囲、妥当な仮定を短く示す。
4. 懸念と具体例：懸念には「具体例1」「具体例2」のように番号を付け、入力例・期待結果・危険な結果を示す。
5. 実装と検証：関係ファイルだけを確認し、専用テスト、必要なビルド、UI変更なら実ブラウザー操作を行う。
6. コミットと報告：意図したファイルだけをコミットし、変更内容・確認結果・未解決点・関係する管理IDを報告する。公開やpushは別途明示された場合だけ行う。

実行上の「次の改善を選ぶ→実装→検証→停止」と、ユーザー向けの「次にやること・方針・懸念・具体例」は別サイクルとして並行させず、この標準表示に統合する。

## バグ修正時の回帰防止

- バグ修正は、再現 → 根本原因の特定 → 既存テストの確認を先に行い、既存テストで検出できない場合は、修正前に最小の回帰テストを追加して可能な範囲でREDを確認する。
- 回帰テストは既存の責務に沿うテストへ統合し、同じ不具合を検出するテストの重複や、テスト数だけを増やす追加は行わない。
- 最小修正後は、追加・関連テスト、`npm run verify`、必要な`npm run build`を実行する。UI・状態遷移の不具合は、ブラウザー接続が可能なら実操作でも修正前後を確認する。
- 環境障害、Codex／ブラウザー自動化自体の障害、仕様未確定、見た目だけの軽微な問題など、テスト追加が不適切な場合は無理に追加せず、理由を最終報告へ記載する。
- 仕様判断、意図不明な既存変更との衝突、OS権限変更、外部認証、Codex再起動、破壊的Git操作が必要な場合を除き、再現から全体検証まで途中確認を求めず継続する。

## 実ブラウザE2Eの選択基準

- DOM・CSS・イベント配線、保存／復元、状態遷移、プレビュー、確定譜面、またはUIで再現するバグを変更した場合は、関連する最小フローを実ブラウザーで確認する。変換器・parserなどUI境界を含まない変更は、既存テストで十分ならブラウザーを起動しない。
- 変更領域ごとの最小フローは次のとおりとする。
  - 変換ロジック：入力 → 変換 → 変換結果の行・記号を確認。UI接続を変更していなければテストのみでよい。
  - 設定変更：入力 → 変換 → 設定変更 → 出力更新と手動編集保持を確認。
  - 手動編集・override：入力 → 変換 → 変換後編集 → 再変換で編集保持を確認。
  - 行追加・削除：入力 → 変換 → 行操作 → 再変換で対象行とoverrideの対応を確認。
  - 履歴・JSON：保存／エクスポート → 一覧または再読込 → 復元 → 入力・出力・設定の一致を確認。
  - preview・確定譜面：変換 → プレビュー表示設定または移調 → 確定 → 確定テキストとの一致を確認。
  - レイアウト・UI：対象幅でページを開き、変更した操作を実クリック・入力し、表示・aria属性・遷移先・反映先を確認。
- 成功条件は、ユーザー操作が実行でき、期待する表示または状態が更新され、関連する出力・保存値が一致し、コンソールエラーがないこととする。
- セレクタは表示中のrole・label・操作名・安定したIDを優先し、座標・DOM順・固定待機時間に依存しない。操作後は待ち時間ではなく、表示・値・toast・URLなどの状態を確認する。
- 接続失敗、起動失敗、ダイアログ処理、trusted pathなどブラウザー環境のエラーと、DOM不在、誤表示、状態不整合、コンソールエラーなどアプリのエラーを分けて記録する。環境エラーだけでアプリ不具合と判定しない。
- 公開前の全項目確認は `chordwiki-release-audit` Skillに任せ、通常開発ではこの基準で必要なフローだけを選ぶ。実UIで発見した再現可能な不具合は、上記のバグ修正ルールに従って既存テストへ還元する。

# Codex / Subagent 運用

## 基本方針

- 目的は、作業品質を保ちながら推論コストとコンテキスト消費を抑えること。
- 親エージェントは、ユーザー要求の解釈、作業分解、担当の割当、結果の統合、最終判断、最終diffの確認を担当する。
- サブエージェントは、探索、調査、実装、テスト、レビューなど、範囲を明確に切り出せる作業を担当する。
- サブエージェントに仕様の最終決定や、ユーザー意図の変更を委ねない。判断が必要な場合は、根拠・不確実性・変更案を返させる。
- サブエージェントを使うこと自体を目的にしない。短い検索、単一ファイルの小修正、単純なテスト実行は親エージェントが直接行う。

## 役割とモデル選択

モデル名や推論設定は、現在のCodex環境で実際に利用可能なものだけを使う。利用可能性を確認できないモデル名や `.codex` 設定を新規に推測して追加しない。

利用可能な場合の役割分担は次の順序を基本とする。

- 通常の探索・実装・テスト・差分確認：標準または低コストのモデル。
- 複数ファイルにまたがる変更、失敗原因の切り分け、parser / adapter / renderer 間の問題：より高性能なモデルへエスカレーション。
- 仕様の曖昧さ、設計変更、互換性・回帰リスクが高い変更、最終判断：親エージェントまたは最上位の推論モデル。

モデル選択は1回あたりの性能ではなく、タスク完了までの再試行回数、検証コスト、回帰リスクを含めて判断する。低コストモデルで同じ失敗を繰り返さない。

## エスカレーション

- 最初は最小限の担当範囲で1体のサブエージェントに依頼する。
- 同じ原因の修正を、指示を少し変えただけで同じモデルへ繰り返し依頼しない。
- 調査不足、複数モジュールに及ぶ問題、設計判断が必要な問題、または検証失敗が続く場合だけ上位モデルへ引き上げる。
- エスカレーション時は、既に確認した事実、失敗した試行、残っている問い、対象ファイルを引き継ぐ。全履歴を無制限に再投入しない。
- 最終的なコード変更、テスト結果、差分は親エージェントが必ず確認する。

## 並列化

並列化は、担当範囲と出力が独立している場合だけ行う。例：実装箇所の探索と既存テストの調査、または異なる機能領域の独立したレビュー。

- 同じファイル・同じ原因・同じ調査対象を複数体に重複依頼しない。
- 並列実装で同一ファイルを編集させない。必要なら探索・レビューを並列化し、実装は親エージェントが統合する。
- 通常は1体、明確な独立作業がある場合でも必要最小限にする。
- 並列結果は、重複・矛盾・根拠の有無を親エージェントが確認してから採用する。

## ChordWiki固有の高リスク領域

次の変更は通常のUI修正より高リスクとして扱う。

- コードバー変換、拍子・小節処理、行記号、ハイフン、コード位置
- parser / adapter / renderer の境界
- 保存データとの互換性、ChordWiki向け出力、既存入力の解釈
- 回帰影響が広いリファクタリング

これらは、調査だけを低コストモデルに任せ、仕様判断・実装方針・最終レビューを上位モデルまたは親エージェントが担う。変更後は関連テスト、ビルド、必要に応じて手動確認を行う。

## サブエージェントへの依頼形式

依頼には、目的、対象範囲、変更してよいファイル、禁止事項、確認コマンド、期待する報告形式を含める。報告は次の形式を基本とする。

1. 確認した事実
2. 変更内容または推奨案
3. 実行した検証と結果
4. 未解決点・リスク
5. 関係する管理ID

サブエージェントが変更を行った場合も、親エージェントが管理ID、diff、テスト結果を確認し、最終報告に反映する。
