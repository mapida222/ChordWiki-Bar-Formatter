# プロジェクト共通の管理ID

このプロジェクトで修正、動作確認、設計判断を報告するときは、`MANAGEMENT_IDS.md` を参照する。

- 同じ機能・確認項目には、チャットが変わっても既存の管理IDを再利用する。
- 新しい項目には、該当カテゴリの次の連番を割り当てて `MANAGEMENT_IDS.md` に追記する。
- 既存IDは改番・再利用・削除しない。廃止した場合は状態を「廃止」として残す。
- 最終報告には、関係する管理IDを必ず記載する。
- ユーザーが管理IDを指定した場合は、その台帳項目を作業範囲の基準にする。
- コード位置が役立つ場合は、管理IDに加えてファイル名と行番号も示す。
- Gitコミットを作成するときは、コミットメッセージの先頭に主な管理IDを角括弧で付ける（例：`[ROW-003] 行修正記号を修正`）。
