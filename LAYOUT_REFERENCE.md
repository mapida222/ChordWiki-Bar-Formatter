# レイアウト設定書

更新日：2026年7月22日

この文書は、ChordWiki Bar Formatterで調整済みのレイアウト値と、変更時に守る関係を記録する。
実装と記載が異なる場合は、`style.css`と`js/chordwiki-preview.js`の実装を優先する。

現在の譜面プレビューレイアウトを「ベスト条件」とする。
比較・復元に使う基準は`layout-snapshots/2026-07-22-good`である。

## 次回の読み出し方

Codexへ「`LAYOUT_REFERENCE.md`を読んでからレイアウトを修正」と依頼する。
譜面プレビューを変更した場合は、この文書とプレビューテストの期待値も更新する。

「ベスト条件と比較して」と依頼された場合は、`layout-snapshots/2026-07-22-good`と現行ファイルの差分を確認する。

レイアウトが崩れた場合は、正常時点の[`layout-snapshots/2026-07-22-good`](layout-snapshots/2026-07-22-good/RESTORE.md)と現行ファイルを比較する。
スナップショットは全上書きせず、レイアウトに関係する差分だけを復元する。

## トップページの配置

- 04「行修正」と05「変換後」の編集枠上端をそろえることを最優先とする。
- 02「変換前」の下枠と05の開始位置の行間は`0`とする。
- 01「表示設定」が02より高い場合、02は1段目の下端へ固定し、05の見出し直上へ配置する（`.input-card { align-self: end; }`）。
- 02の高さを変更しても、02と05を離すための余白を追加しない。
- 03「初期設定」は、開閉状態にかかわらず04と05の位置へ影響させない。
- 03は04へ重なってよい。
- 04と05の編集枠上端の差は`--result-controls-offset`だけで補正する。
- 06以降の段との間隔は`12px`を維持する。
- 「ひとことサポート」は「入力できる文字列」と同じ左列スタックに置き、入力できる文字列枠の高さ変更へ追従する。2枠の間隔は`12px`とする。

## 初期設定の表示

設定項目の先頭は、次の順にする。

1. 1小節の合計ハイフン数
2. コード直後のハイフン数
3. ハイフンを空白で区切る間隔

「ハイフンを空白で区切る間隔」は`0`を選択できる。
`0`は、連続するハイフンを空白で区切らない設定である。

## 譜面プレビューの基本値

| 対象 | 現在値 | 目的 |
| --- | ---: | --- |
| 譜面1行の上下余白 | `1em 0` | コード行と前後行を分離する |
| 譜面1行の行高 | `1.5` | コードと歌詞を読みやすくする |
| コード行の縦位置 | `top: -1.1em` | コードを歌詞の上へ配置する |
| 歌詞本体の横位置 | `left: -.8em` | ChordWiki本家の文字位置へ近づける |
| 通常の小節線 | `left: .4em` | 小節線を基準位置から右へ置く |
| コード行の後置小節線 | `left: .6em` | 通常位置から右へ`0.2em`動かす |
| 歌詞を含む行の後置小節線 | `left: -.2em` | 以前の`-.4em`から右へ`0.2em`動かす |
| 歌詞本文内の小節線 | `left: .4em` | 歌詞行の小節線位置をそろえる |
| 行頭の小節線 | `left: .4em` | 歌詞行の頭を以前の位置から右へ`0.5em`動かす |

## 小節線の間隔

| 関係 | 現在値 | 適用範囲 |
| --- | ---: | --- |
| 行頭小節線の右余白 | `.28em` | コードを伴わない行頭小節線 |
| `[|][コード]`の右余白 | `.08em` | 小節線の直後がコードの場合 |
| 歌詞行の行頭小節線の右余白 | `.78em` | 行頭小節線を右へ動かした分を補う |
| 歌詞行の`[|][コード]`の右余白 | `.58em` | 上記からコードとの間を`0.2em`狭める |
| 後置小節線の左余白 | `.18em` | コード行の小節線手前 |
| 後置小節線の右余白 | `.28em` | 小節線直後のコードとの間 |
| `----[|]`の追加余白 | `.3em` | 最後のハイフンと小節線の接触を防ぐ |
| `|(2/4)`の追加余白 | `.4em` | 小節線と右側文字の重なりを防ぐ |

## 表示例ごとの規則

### `[|] |`

`[|]`はコード行、後ろの`|`は歌詞行の小節線として扱う。
位置合わせ用の先頭空白はプレビューから除き、上下の縦棒を同じ横位置へ置く。
二本の縦棒は、一本の貫通線に見える状態を保つ。

### `[|][DbM7]く`

行頭小節線は右へ`0.5em`動かす。
小節線と`DbM7`の間は、通常の行頭小節線より`0.2em`狭い`.58em`とする。
小節線とコードや歌詞を接触させない。

### `----|(2/4)`

歌詞本文内の小節線だけを右へ動かす。
小節線の右側にある`(2/4)`は小節線へ寄せない。
小節線へ`.4em`の右余白を付け、カッコとの重なりを防ぐ。

### `[----][|]`

最後のリズム記号がハイフンで、直後がブラケット形式の小節線の場合だけ`.3em`を追加する。
コード、小節線、歌詞の通常間隔には追加しない。

## 使い方ページ

使い方ページは、画面番号ではなく利用者の目的を中心に分類する。

1. ダミー歌詞による入力と変換後の完成イメージ
2. 入力・修正・コピーの最短3ステップ
3. 画面遷移しない「困ったとき」の回答
4. 「入力する・ズレを直す・仕上げて残す」の目的別ガイド
5. 共通の操作アイコン

著作物の歌詞は例に使用しない。「もう一例」の二重折りたたみとヘルプ内ジャンプは設けない。表示設定の説明は省略する。余白は小さく保ち、スマートフォンでは縦方向へ並べる。

## 確認テスト

譜面プレビューの変更後は、次のテストを実行する。

```powershell
node tests/preview.test.js
node tests/preview-layout.test.js
```

確認対象の代表例は、`[|] |`、`[|][DbM7]く`、`----|(2/4)`、`[----][|]`である。
## 入力できる文字列

表示順は `0～9`、`a～i`、`*`、`^`、`@`、`s`、`|`、`x`、`n`。

- `|` は、その前後を含む全コードへ指定値を適用し、記号直後のコードを小節頭にする。指定行の先頭には小節線を自動追加しない。
- `x` は通常の小節容量による区切りより優先し、対象の小節線を消す。
